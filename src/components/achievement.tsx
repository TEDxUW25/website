import React from 'react';

const Achievement: React.FC = () => (
  <section className="w-full bg-black h-40 flex items-center justify-center">
    <span className="text-white text-2xl font-bold">Achievement Section Placeholder</span>
  </section>
);

export default Achievement; 